void DuomenysInternet(Grybai & grybai)
        {
            
            StreamReader sw = new StreamReader("text");
            // string pav, tip;
            // double s1;
            int ns = 0;
            /* bool yra = true;
            while(!sw.EndOfStream && yra) { // kol yra duomenų ir jie telpa į masyvą
                pav = sw.ReadLine();
                Console.Write(pav);

                if (!sw.EndOfStream && (ns - 1 < pav.Length))
                    Console.WriteLine();
                /*else
                    yra = false;
                */
                string helloWorld = /*pasauli */ "World";
            
            }

            sw.close();
         }